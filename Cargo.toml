[workspace]
resolver = "2"
members = ["contracts/savings"]
exclude = [
  "node_modules",
  ".next",
  "src",
  "scripts",
  "docs",
]

[workspace.dependencies]
soroban-sdk = "25"
stellar-sdk = "15"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
